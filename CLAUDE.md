# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Status

This is an early-stage ETL (Extract, Transform, Load) project. The repository has just been initialized and has no source code, build configuration, or test infrastructure yet.

## Project Context

- **Name**: ETL_TEAM
- **Purpose**: ETL pipeline (details to be established as development begins)
- **IDE**: Visual Studio (`.vs/` directory present)

## Notes for Future Development

As the project grows, update this file with:
- Build commands (e.g., `dotnet build`, `npm run build`)
- Test commands (e.g., `dotnet test`, `npm test`)
- Lint/format commands
- Architecture overview once components are established
- Environment setup instructions
